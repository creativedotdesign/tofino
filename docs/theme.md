# Theme

Shared PHP code lives here.

- `core/` theme setup and asset loading
- `utils/` shared helper functions
- `registry/` feature and module discovery
- `integrations/` external tooling and service integrations

Feature-specific or module-specific code should live with that feature or module first.
