# Source

Front-end source files live here.

- `js/` TypeScript
- `css/` styles
- `public/` static assets copied as-is
- `sprite/` shared SVG sprite sources

Feature and module assets stay in their own folders when they belong to a specific feature or module.
